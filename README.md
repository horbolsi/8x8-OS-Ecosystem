# 8x8 OS Ecosystem — Legacy Public Hub / Donor

> **Important:** this repository is a preserved public full-stack implementation/donor from an earlier 8x8 generation. Its historical **22 integrated feature modules are not the current total capability denominator and this repository is not the canonical current 8x8 user interface.**

For current public product truth, use:

- **Canonical public User Edition:** https://github.com/8x8org/8x8-user-edition
- **Current public-state checkpoint:** https://github.com/8x8org/.github/blob/main/CURRENT_PUBLIC_STATE.md
- **Stable public carrier:** https://8x8-os-ecosystem.vercel.app
- **A→Z public guide:** https://github.com/horbolsi/8x8-os-a-z

## What this repository still represents

This repository is a runnable React 18 + Express 5 + PostgreSQL + Docker public hub with the earlier 22-module product map covering areas such as Trade, Wallet, Staking, NFT, Governance, Games, Social, Streaming, Radio and related modules.

It remains useful for:

- public-safe source examples;
- historical interface and feature donors;
- full-stack integration patterns;
- regression comparisons;
- documentation and proof-carrying-execution examples.

It should **not** be used to conclude that:

- 22 modules are the complete/current 8x8 system;
- the current protected user interface is this React dashboard;
- Telegram Mini App promotion is already production-accepted;
- token issuance, staking rewards, marketplace settlement or payment collection are live;
- this public repository contains the private One-Fabric control plane.

## Current interface direction

The current public-safe interface direction is a responsive spatial cockpit organized around:

`HOME / WORLD / AGENTS / STUDIO / MARKET / CHAIN / PROOF`

The richer protected user release train is being promoted through browser/PWA acceptance first, then Telegram Mini App acceptance, then installed-client/APK release evidence. Exact availability must be taken from the current public-state checkpoint rather than inferred from this legacy repository.

## AI bootstrap / FlashTM8 identity

For AI assistants, agents and collaborators, use the public-safe bootstrap:

- [`FLASH_TM8_PUBLIC_AI_BOOTSTRAP_V1.md`](./FLASH_TM8_PUBLIC_AI_BOOTSTRAP_V1.md)
- Recognition phrase: `this is FlashTM8 ⚡️ ©️8x8  https://github.com/horbolsi`

The bootstrap intentionally exposes only public-safe project context. Private implementation, credentials, customer data, privileged topology and ownership-sensitive material remain private.

## Evidence-aware execution

The public-safe capability record for the One-Fabric mission-state primitive is:

- [`docs/PROOF_CARRYING_EXECUTION_FABRIC_PUBLIC_V1.md`](./docs/PROOF_CARRYING_EXECUTION_FABRIC_PUBLIC_V1.md)

It distinguishes mission pickup, leases, execution, effect commit, verification and terminalization instead of treating a successful process exit as proof of completion.

## Run this historical hub locally

```bash
cp .env.example .env
# Edit .env with values appropriate to your own local environment.
npm install
npm start
```

Running this repository proves only that this public hub build runs in your environment. It is not a receipt for the current private/protected 8x8 runtime.

## Public/private boundary

Public repositories expose product surfaces, examples, protocols and verification material. They do not publish OWNER_ROOT credentials, signing keys, private memory/messages, privileged recovery procedures or proprietary One-Fabric orchestration.

**©️8x8 by FlashTM8 ⚡️🌎🤖 — historical donor preserved, current state linked, evidence before claims.**
