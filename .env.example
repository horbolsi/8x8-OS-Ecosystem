# Public-safe compatibility server
HUB_PORT=3000
NODE_ENV=production
ALLOWED_ORIGINS=http://localhost:8086,http://localhost:3000

# No administrator, bot, wallet, trading, database, or provider credentials
# are required or accepted by the public companion server.
